/**
 * 注册
 * @param {*} ctx 
 */
async function register(ctx) {
}

/**
 * 登陆
 * @param {*} ctx 
 */
async function login(ctx) {
}

/**
 * 判断用户是否进行过授权登录
 * @param ctx 
 */
async function authorization(ctx) {
}

/**
 * 判断用户是否进行过授权登录
 * @param ctx 
 */
async function setAuthorization(ctx) {
}

export {
    login,
    register,
    authorization,
    setAuthorization
}